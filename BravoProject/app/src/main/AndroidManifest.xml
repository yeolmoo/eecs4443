<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BravoProject"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConditionSelectActivity" />
        <activity android:name=".MenuTestActivity" />
        <activity android:name=".BottomNavActivity" />
        <activity android:name=".TopNavActivity" />
        <activity android:name=".FabActivity" />
        <activity android:name=".SwipeMenuActivity" />
        <activity android:name=".HamburgerMenuActivity" />
        <activity android:name=".RadialMenuActivity" />
        <activity android:name=".InstructionActivity" />
    </application>

</manifest>